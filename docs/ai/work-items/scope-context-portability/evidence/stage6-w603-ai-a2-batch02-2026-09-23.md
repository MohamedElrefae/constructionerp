# Stage 6 W6-3 batch-02 — AI-A2 domain review (2026-09-23)

- Reviewer: AI-A2 stock-domain terminology reviewer
- Independent session: `/root/w603_a2`
- Model: GPT-6
- Final reviewed proposal: `docs/translation/stage6_w603_proposal_batch02_2026-09-23.csv`
- SHA-256: `aaac0e04b364f63e55dcfbd59bd72a19156952523f500226cb919aa387a8349e`
- Scope binding: owner-approved 250-row CSV `docs/translation/stage6_w603_batch02_rows_2026-09-23.csv`, SHA-256 `701ad13e6b4cc530ebd7dbd95705979b70055c46f39da37d4532a26ba60439ef`.

## Verdict

**PASS** — all 103 proposed release rows approved for stock-domain correctness. Previous wording finding on “Linked with submitted documents” was resolved as `مرتبط بمستندات مُقدَّمة`.

## Source defects deferred

Both rows are excluded from payload and remain blank pending vendor-side correction, which is outside this scope:

1. `Reserved Qty ({0}) cannot be a fraction. To allow this, disable '{1}' in UOM {3}.` references `{3}` although the inspected formatter provides only positional arguments `{0}`–`{2}`.
2. `Row # {0}: Please enter quantity for Item {1} as it is not zero.` is raised inside a `not item.current_qty` condition, contradicting its text.

## Preservation-only risks

The following existing Site Override values were flagged but not changed or included in release approval: `Recalculate Bin Qty` (Bin meaning), `Repost Item Valuation` (reposting action), and `Reserved for Sub Contracting` (glossary terminology). Existing values remain preserved exactly per plan §12.

## Checks

- Reviewed all 250 scope rows, including preserve/technical/deferred classifications; PASS applies to the 103 candidate releases.
- Exact proposal SHA above is the verdict binding; any proposal-byte change invalidates it.
