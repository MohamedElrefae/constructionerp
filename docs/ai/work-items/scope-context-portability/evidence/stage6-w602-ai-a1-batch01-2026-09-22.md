# Stage 6 W6-2 batch-01 — AI-A1 linguistic review (2026-09-22)

Session: `/root/ai_a1_arabic` (recorded review run)
Role: AI-A1 linguistic reviewer — independent of Builder/proposer.
Input: owner-approved `docs/translation/stage6_w602_batch01_rows_2026-09-22.csv` (sha `b017df4787ff8754be0bf43425151b748ba0aa019348fa29187f660130408fae`) + proposal panel `stage6_w602_ai_proposal_build_batch01_2026-09-22.py` output (86 Released rows: 85 AI-proposed unfilled + 1 pre-filled ` Address`).

## Verdict

**APPROVE** all **86** Released rows (Egyptian professional Arabic; placeholder parity; no source-equal on Released; no HTML-unsafe; no affix drift).

## Import-time amendment (2026-09-23)

- **` Address` reclassified** out of the Released payload at governed import: `upsert_runtime_translation` strips edge whitespace; stripped key `Address` has an existing `ct_origin='Site Override'` runtime row (plan §12 preserve). Catalog total **2276 → 2275**. Remaining Released set for this batch = **85**. Linguistic APPROVE of the original proposal text stands; disposition is §12, not linguistic rejection.

## Exceptions acknowledged

- **184** `preserved-site-override` (plan §12) — 183 exact-key SOs from recon + 1 import-time strip-collision (` Address` → runtime `Address` SO).
- **1** `already-released` — `Advance Payment` already Released in the managed catalog (`دفعة مقدمة`); catalog row retained, not re-appended.
- **0** `EXCEPTION-technical` — no format/identifier/brand/protocol-only tokens in this batch after review.

Accounting: 184 + 1 + 0 + 85 = 270 (fail-closed; recon disposition amended).

## Review checks

- Placeholder parity on all `{…}`/`{}`/`{0}`-style rows: pass (build asserts).
- Distinct Arabic ≠ source on all 86 Released: pass.
- Glossary v2.0: Buying/Selling settings labels use established Egyptian accounting terms (`أمر شراء`, `أمر بيع`, `إيصال الشراء`, `فاتورة الشراء`, `مقاولة باطن`).
- Decision binding: each Released catalog row binds to `content:docs/translation/stage6_w602_payload_applied_rows_batch01_2026-09-22.csv` (raw-text source+translation substring).
