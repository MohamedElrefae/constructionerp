# Stage 6 W6-2 batch-02 — AI-A1 linguistic review (2026-09-23)

Session: `/root/ai_a1_arabic` (recorded review run)
Role: AI-A1 linguistic reviewer — independent of Builder/proposer.
Input: owner-approved `docs/translation/stage6_w602_batch02_rows_2026-09-23.csv` (sha `9a096273f2f7ef05a403edffbb4b75604b73ce7f854382c8fd570f5c329e44ce`) + proposal panel `stage6_w602_ai_proposal_build_batch02_2026-09-23.py` output (44 Released rows).

## Verdict

**APPROVE** all **44** Released rows (Egyptian professional Arabic; placeholder parity; no source-equal on Released; no HTML-unsafe; no affix drift).

## Exceptions acknowledged

- **1** `preserved-site-override` (plan §12) — exact-key SO on `Address` (`العنوان`); runtime value matches scope pre-fill; not re-catalogued.
- **3** `EXCEPTION-technical` — `doctype`, `doc_type`, `quotation_item` (snake_case field identifiers; keep vendor rendering).
- **0** `already-released` — no batch-02 key was already Released in the managed catalog (0 overlap with 2275).

Accounting: 1 + 3 + 0 + 44 = 48 (fail-closed).

## Review checks

- Placeholder parity on all `{…}`/`{}`-style rows: pass (build asserts); `{}  To Deliver` keeps empty-brace pair.
- Distinct Arabic ≠ source on all 44 Released: pass.
- Glossary v2.0: Selling desk terms — `أمر بيع` (Sales Order), `فاتورة البيع` (Sales Invoice), `نقطة البيع` (POS), `المورد` (Supplier), `أمر البيع المفتوح` (Blanket Order), `مقاولة الباطن` (Subcontracting), `قائمة المواد` (BOM), `المستودع` (Warehouse).
- Decision binding: each Released catalog row binds to `content:docs/translation/stage6_w602_payload_applied_rows_batch02_2026-09-23.csv` (raw-text source+translation substring).
