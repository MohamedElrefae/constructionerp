# Stage 6 W6-2 batch-02 — AI-A2 domain review (2026-09-23)

Session: `/root/ai_a2_domain` (recorded review run)
Role: AI-A2 Egyptian construction/accounting domain reviewer — independent of Builder and A1.
Input: owner-approved `docs/translation/stage6_w602_batch02_rows_2026-09-23.csv` (sha `9a096273f2f7ef05a403edffbb4b75604b73ce7f854382c8fd570f5c329e44ce`) + proposal panel output (44 Released rows).

## Verdict

**ACCEPT** all **44** Released rows for Egyptian Selling desk usage.

## Domain notes

- Selling vocabulary: `أمر بيع` (Sales Order), `عرض سعر` (Quotation), `فاتورة البيع` (Sales Invoice) — consistent with glossary v2.0 and Stage-1C/4 / W6-2 batch-01 releases.
- POS lifecycle: `نقطة البيع` retained for POS; opening/closing entry messages use established wording from batch-01 (`قيد فتح نقطة البيع`).
- Delivery schedule / credit limit / over-picking settings labels are configuration + validation UI — standard ERPNext Arabic.
- `مقاولة الباطن` retained for Subcontracting (construction-primary term; prior site-approved usage).
- Identifier tokens `doctype` / `doc_type` / `quotation_item` correctly withheld from translation (EXCEPTION-technical).
- Dual-meaning flags: none requiring deferral in this 44-row set.

## Exceptions acknowledged

- **1** preserved site override (`Address` — plan §12).
- **3** EXCEPTION-technical identifier tokens.
- **0** already-released.

Accounting: 1 + 3 + 0 + 44 = 48.

## Decision binding

Each Released catalog row binds to `content:docs/translation/stage6_w602_payload_applied_rows_batch02_2026-09-23.csv`.
