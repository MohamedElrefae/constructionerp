# Execution addendum to canonical r5

The owner's attached consultant directive authorizes Phase 0 exit and Phase 1 work, approves the exact five role-prompt hashes, and permits an independent native Codex/OpenCode plan-review session while `agy` is unavailable. This is an explicit execution amendment to the original Antigravity default; canonical r5 and its recorded hashes remain unchanged.

The implementation recognizes `/usr/bin/opencode-cli` directly, avoiding a global symlink change. Codex uses the already-probed bundled executable. `roles.json` pins observed versions, executable hashes, prompt hashes and model selections. Antigravity availability remains honestly recorded as absent.

Owner-executed commands alone do not establish exclusive control on a shared OS account. The implementation therefore verifies a concrete Bubblewrap write boundary, hides peer/control artifacts, and retains owner-executed commit/data operations. Read-only mounts are tested and native transports were tested inside the same wrapper. The trusted owner boundary includes unsandboxed processes using the same OS account. No claim of universal same-user isolation is made.

The consultant's statement that the original ERP checkout is clean is incorrect: it has 59 dirty entries, consistent with the earlier uncommitted ERP work. This work has not cleaned, staged, committed or modified that checkout.

Phase 1 added an independent native code review before freezing. Automatic approval review initially rejected sending internal source/plans to the external Codex provider without explicit payload/destination authorization. The owner subsequently authorized that scoped transfer in this task, excluding private ERP data and credentials. The independent read-only review was then launched in native session `01a08d85-70fd-7183-b6fc-2261a1f40938`. Its findings and remediation are recorded in the implementation evidence. This source review is separate from the harmless native transport probes and does not authorize a commit, pilot or ERP operation.

The initial independent review found four P1s. A bounded follow-up accepted three repairs and identified a related stale-checkpoint approval replay case. After that repair, the final independent native check returned PASS in session `01a08d90-02e9-7113-beb5-040d1bbcb4de`. All reports remain preserved; no finding was erased or counted as pilot evidence. The Phase 1 source freeze and 93-test verification are recorded in the evidence directory.
