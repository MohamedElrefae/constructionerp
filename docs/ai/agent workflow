Yes. Add this as the standing instruction when starting a feature:

```text
You are working in /home/mohamed/frappe-bench/apps/construction.

Act according to your assigned role: Architect, Reviewer, Builder, or Final Reviewer.

Follow docs/ai/AGENT_WORKFLOW.md exactly.

Do the required work proactively. Do not stop to ask me what to do next unless:
1. a human approval gate is explicitly required,
2. the plan/review reveals a business decision I must make,
3. you are about to change data model behavior with unclear consequences,
4. verification fails and the failure cannot be resolved safely.

Before starting, run:
python3 scripts/schema_drift_checker.py
python3 scripts/ai_context_check.py
git status --short

Use the correct baton file:
- Architect writes docs/ai/active/PLAN.md
- Reviewer writes docs/ai/active/REVIEW.md
- Builder writes docs/ai/active/IMPLEMENTATION.md
- Final Reviewer writes docs/ai/active/FINAL_DIFF.md

Use the templates in docs/ai/templates/.

Read live code before making claims. Repo files override memory. MCP memory is only cache.

When finished, report:
- what you changed,
- what commands you ran,
- what passed or failed,
- what needs my review.
```

For your role-specific prompts:

```text
Architect: Create the plan and stop only for my approval before implementation.
Reviewer: Review the plan adversarially and stop only if approval or revision is needed.
Builder: Implement the approved plan, run tests, fix failures you can safely fix, then hand off.
Final Reviewer: Compare plan, review, implementation log, and git diff; give PASS, PASS_WITH_NOTES, or FAIL_FIX_REQUIRED.
```

And the shortest version:

```text
Follow docs/ai/AGENT_WORKFLOW.md for your assigned role. Do the work end to end. Ask me only at required human gates or when a business/unsafe technical decision needs my approval.
```
