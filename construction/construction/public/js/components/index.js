/**
 * Modern Theme Components Bundle
 * All React components for the Modern UI Theme
 */

// Export all components
window.ModernThemeComponents = {
	UnifiedCRUDForm: window.UnifiedCRUDForm,
	FormField: window.FormField,
	FormLayoutControls: window.FormLayoutControls,
	DraggablePanel: window.DraggablePanel,
	TreeView: window.TreeView,
	Sidebar: window.Sidebar,
	ExportButtons: window.ExportButtons,
	UltimateButton: window.UltimateButton,
};

// Log ready status
console.log("[Modern Theme] Components loaded successfully");
